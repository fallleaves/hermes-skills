# OS
.DS_Store
Thumbs.db

# Editor
*.swp
*.swo
.vscode/

# Temp files
*.tmp
*.bak

# Sensitive data (if any skills store credentials)
credentials/
*.pem
*.key
.env

# Python
__pycache__/
*.pyc

# Hermes runtime usage tracking (auto-updated on every skill load)
.usage.json

# Runtime/temp state - never push
.usage.json.lock
.curator_ledger.jsonl
# curator's weekly backup bundles (tar.gz) — runtime state, never push
.curator_backups/
.hub/
# skills_sync rewrites this on every bundled-skill sync (skill_name:origin_hash)
.bundled_manifest
